# ----------------------------
# Server Configuration
# ----------------------------
server.port=8080
spring.application.name=boarding-house-backend

# ----------------------------
# Database Configuration (MySQL)
# ----------------------------
spring.datasource.url=jdbc:mysql://localhost:3306/boarding_house_db?useSSL=false&serverTimezone=UTC
spring.datasource.username=root
spring.datasource.password=Senzai.3352048663653
spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver

# ----------------------------
# JPA / Hibernate Configuration
# ----------------------------
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.format_sql=true
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.MySQLDialect
spring.jpa.properties.hibernate.jdbc.time_zone=UTC

# ----------------------------
# JWT Configuration
# ----------------------------
jwt.secret=5367566B59703373367639792F423F4528482B4D6251655468576D5A71347437
jwt.expiration=86400000

# ----------------------------
# Google OAuth2 Configuration
# ----------------------------
spring.security.oauth2.client.registration.google.client-id=${GOOGLE_CLIENT_ID}
spring.security.oauth2.client.registration.google.client-secret=${GOOGLE_CLIENT_SECRET}
spring.security.oauth2.client.registration.google.scope=profile,email
spring.security.oauth2.client.registration.google.redirect-uri=http://localhost:8080/login/oauth2/code/google

# ----------------------------
# Logging
# ----------------------------
logging.level.com.boardinghouse=DEBUG
logging.level.org.springframework.security=DEBUG

# ----------------------------
# File Upload Configuration
# ----------------------------
spring.servlet.multipart.enabled=true
spring.servlet.multipart.max-file-size=10MB
spring.servlet.multipart.max-request-size=10MB